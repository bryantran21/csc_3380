package main;

import java.util.ArrayList;

public class Classes {
	/*
	 * Class contains an arraylist of the classes a user is in
	 */
	int numOfClasses = 0;
	public static class classClass {
		/*
		 * class for an individual course that has the grade the user has in it, and the name of the course
		 */
		String grade;
		String className;
	}
	ArrayList<classClass> classList = new ArrayList<classClass>();
}
